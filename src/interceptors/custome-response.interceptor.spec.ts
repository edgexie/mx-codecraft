import { CustomeResponseInterceptor } from './custome-response.interceptor';

describe('CustomeResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new CustomeResponseInterceptor()).toBeDefined();
  });
});
